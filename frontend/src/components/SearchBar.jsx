import React, { useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { Search, X } from 'lucide-react';
import { CATEGORY_MAP } from '../constants/categories';
export default function SearchBar({ searchQuery, onSearchChange, totalResults, selectedCategory, loading }) {
  const [focused, setFocused] = useState(false);
  return <section id="esplora" className="page-section search-section"><div><h2>Cerca un’attività</h2></div><div className="search-area"><motion.div className={'search-shell ' + (focused ? 'is-focused ' : '') + (selectedCategory ? '' : 'rainbow-search')} style={{ '--accent': CATEGORY_MAP[selectedCategory]?.colorHex || '#818cf8' }} animate={{ scale: focused ? 1.015 : 1 }} transition={{ type: 'spring', stiffness: 260, damping: 22 }}><motion.span key={searchQuery} initial={{ rotate: -15, y: -2 }} animate={{ rotate: 0, y: 0 }}><Search size={26} /></motion.span><input aria-label="Cerca uno strumento o un’attività STEM" type="search" value={searchQuery} onChange={e => onSearchChange(e.target.value)} onFocus={() => setFocused(true)} onBlur={() => setFocused(false)} placeholder="Prova Arduino, Scratch, robotica…" />{searchQuery && <button onClick={() => onSearchChange('')} aria-label="Cancella ricerca"><X size={19} /></button>}</motion.div><div className="search-results" role="status" aria-live="polite"><AnimatePresence mode="wait" initial={false}><motion.span key={totalResults + '-' + loading} initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }}>{loading ? 'Caricamento delle attività…' : <>Trovati <strong>{totalResults}</strong> risultati</>}</motion.span></AnimatePresence></div></div></section>;
}
